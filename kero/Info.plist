<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- This Info.plist is authored by hand (GENERATE_INFOPLIST_FILE = NO).
	     The generator always forces CFBundleName = $(PRODUCT_NAME), i.e. the
	     lowercase "kero", and the macOS app menu shows CFBundleName verbatim.
	     We want the product/bundle/executable to stay lowercase (bundle id
	     sh.kero, kero.app — the release scripts depend on that name) while the
	     user-visible name is capitalized, so we own the plist and set the
	     Bundle* keys ourselves. Dynamic values use build-setting substitution
	     so they stay in sync with the project. -->

	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>

	<!-- User-visible name. CFBundleName drives the app menu (bold menu next to
	     the Apple menu); CFBundleDisplayName is what Finder/Dock show. -->
	<key>CFBundleName</key>
	<string>Kero</string>
	<key>CFBundleDisplayName</key>
	<string>Kero</string>

	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>

	<!-- Sparkle auto-update configuration.
	     Appcast feed hosted on Cloudflare R2 (releases.kero.sh); see RELEASING.md. -->
	<key>SUFeedURL</key>
	<string>https://releases.kero.sh/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>gv9GLMPjH5yMQkZMFXnoNfHOyL8/7KGzl/jzAqlzZZY=</string>
</dict>
</plist>
