<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Sparkle auto-update configuration. The rest of Info.plist (bundle id,
	     versions, etc.) is still generated by GENERATE_INFOPLIST_FILE and
	     merged on top of these keys at build time. -->

	<!-- Appcast feed. Hosted on Cloudflare R2 (releases.kero.sh); see RELEASING.md. -->
	<key>SUFeedURL</key>
	<string>https://releases.kero.sh/appcast.xml</string>

	<!-- EdDSA (ed25519) public key used to verify update signatures.
	     PLACEHOLDER — decodes to "REPLACE-ME-WITH-REAL-SPARKLE-KEY".
	     Replace with the real key from `generate_keys` before shipping.
	     See RELEASING.md. -->
	<key>SUPublicEDKey</key>
	<string>UkVQTEFDRS1NRS1XSVRILVJFQUwtU1BBUktMRS1LRVk=</string>
</dict>
</plist>
